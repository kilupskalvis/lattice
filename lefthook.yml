pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,json}"
      run: ./node_modules/.bin/biome check --write {staged_files}
      stage_fixed: true
    - name: typecheck
      run: bun tsc --noEmit

pre-push:
  jobs:
    - name: tests
      run: bun test
