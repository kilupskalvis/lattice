pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "{src,tests}/**/*.ts"
      run: source ~/.bash_profile && bunx biome check --write {staged_files}
      stage_fixed: true
    - name: typecheck
      run: source ~/.bash_profile && bun tsc --noEmit
